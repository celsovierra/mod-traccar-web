import React from 'react'; import { Dialog, DialogTitle, DialogContent, DialogActions, Button, Typography } from '@mui/material'; const SmsMarketModal = ({ device, onClose }) => { return (<Dialog open={true} onClose={onClose} maxWidth='sm' fullWidth><DialogTitle>Enviar SMS / WhatsApp - {device?.name}</DialogTitle><DialogContent><Typography>Painel de envio de mensagens e campanhas para o veículo {device?.name}.</Typography></DialogContent><DialogActions><Button onClick={onClose} color='primary'>Fechar</Button></DialogActions></Dialog>); }; export default SmsMarketModal;
